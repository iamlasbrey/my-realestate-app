import { Property } from "@/models/Properties"


interface PropertyEntryProps {
    property: Property
}

const PropertyEntry = ({property}:PropertyEntryProps) => {
  return (
    <div>PropertyEntry</div>
  )
}

export default PropertyEntry